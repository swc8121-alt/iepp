name: TRP 2.0 Unit Tests

on:
  push:
    branches: [main, "agent/**"]
  pull_request:

permissions:
  contents: read

jobs:
  unittest:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run TRP 2.0 unit tests
        working-directory: reference/iepp_vnext
        run: python -m unittest -v test_trp2_unittest.py
